<!DOCTYPE html>
<html>
<head>
  <title>Login - ShadowPanel</title>
  <link href="/css/style.css" rel="stylesheet">
</head>
<body class="bg-gray-900 text-white flex items-center justify-center h-screen">
  <form method="POST" action="<%= requires2FA ? '/auth/login/2fa' : '/auth/login' %>" class="bg-gray-800 p-8 rounded shadow-md w-96">
    <h1 class="text-2xl mb-4 font-bold text-center">ShadowPanel Login</h1>

    <% if (error) { %>
      <div class="bg-red-500 p-2 mb-4 rounded text-center"><%= error %></div>
    <% } %>

    <% if (!requires2FA) { %>
      <input type="text" name="username" placeholder="Email or Username" required class="w-full p-2 mb-4 rounded bg-gray-700 text-white">
      <input type="password" name="password" placeholder="Password" required class="w-full p-2 mb-4 rounded bg-gray-700 text-white">
    <% } else { %>
      <input type="text" name="code" placeholder="2FA Code" required class="w-full p-2 mb-4 rounded bg-gray-700 text-white">
    <% } %>

    <button type="submit" class="w-full bg-indigo-600 hover:bg-indigo-700 p-2 rounded">
      <%= requires2FA ? 'Verify 2FA' : 'Login' %>
    </button>
  </form>
</body>
</html>
